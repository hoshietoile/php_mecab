create table regulations(
  id int(11) auto_increment not null primary key,
  check_surface varchar(20),
  check_hinshi varchar(20),
  check_conjugation varchar(20),
  check_normal varchar(20),
  check_yomi varchar(20)
);
